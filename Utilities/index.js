//userPromp...




//projPromp...

/*const projPromp = readMedata => [
    //console.log(``)


    if(!readMedata.)
]*/


















/*userPromp()
   .then(projPromp)
   .then(readMedata => {
       return markdownGen(readMedata);
   })
   .then(htmlPage => {
       //return WriteFile(htmlPage)
   })
   .then(writeFileResponse => {
       console.log(writeFileResponse);
       return copyFile();
   })
   .then(copyFileResponse => {
    console.lon(copyFileResponse);
   })
   .catch(err => {
    console.log(err);
   });*/